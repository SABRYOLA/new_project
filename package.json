{
  "name": "secureops-platform",
  "version": "1.0.0",
  "description": "Comprehensive Security Operations Center (SOC) web application combining Risk Management and Incident Response",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "seed": "cd backend && node utils/seed.js",
    "test": "cd backend && npm test"
  },
  "keywords": [
    "security",
    "SOC",
    "risk-management",
    "incident-response"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
